services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: pi_agents
      POSTGRES_USER: pi
      POSTGRES_PASSWORD: pi
    ports:
      - "5432:5432"
    volumes:
      - pi_harness_postgres_data:/var/lib/postgresql/data

volumes:
  pi_harness_postgres_data:
